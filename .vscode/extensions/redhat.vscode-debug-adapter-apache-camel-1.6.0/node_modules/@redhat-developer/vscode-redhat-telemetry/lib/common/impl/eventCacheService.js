"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.EventCacheService = void 0;
class EventCacheService {
    constructor(fileService) {
        this.fileService = fileService;
        this.memCache = new Map();
    }
    async get(key) {
        if (this.memCache.has(key)) {
            return this.memCache.get(key);
        }
        const value = await this.fileService.readFromFile(`${key}.txt`);
        if (value) {
            this.memCache.set(key, value);
        }
        return value;
    }
    async put(key, value) {
        this.memCache.set(key, value);
        await this.fileService.writeToFile(`${key}.txt`, value);
        return true;
    }
}
exports.EventCacheService = EventCacheService;
//# sourceMappingURL=eventCacheService.js.map